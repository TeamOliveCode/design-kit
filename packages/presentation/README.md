# @olivekit/presentation

On-brand HTML slide decks (PPT-style) on the OliveKit tokens, so an internal deck reads as OliveLabs like everything else. `deck.html` is a self-contained, keyboard-navigable template: link the token CSS, edit the slides, share the file or a link. No build, no framework. See `docs/standards/presentation.md`.
